<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
</head>
<body>
  <h1>Login</h1>
  <form action="/login" method="post">
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>
    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required>
    <button type="submit">Login</button>
  </form>
  {{#if error}}
  <p>{{error}}</p>
  {{/if}}
  <p>¿No tenes una cuenta? <a href="/register">Regístrate</a></p>
   <p><a href="/forgot-password">Olvidé mi contraseña</a></p> <!-- Enlace a la página de recuperación de contraseña -->
  <!-- Botón de inicio de sesión con GitHub -->
  <a href="/auth/github">Iniciar sesión con GitHub</a>
</body>
</html>